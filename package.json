{
  "name": "blue-crm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "CRM Blue Contabilidade",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node copy-scripts.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
